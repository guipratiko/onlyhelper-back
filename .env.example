PORT=4000
NODE_ENV=development
# Em produção: defina NODE_ENV=production e use JWT_SECRET forte (ex.: openssl rand -base64 32)
# Se o frontend estiver em outro domínio, liste as origens permitidas (separadas por vírgula):
# CORS_ORIGIN=https://seusite.com,https://www.seusite.com
MONGODB_URI=mongodb://user:password@host:port/onlyhelper?tls=false
POSTGRES_URI=postgres://user:password@host:port/onlyhelper?sslmode=disable
JWT_SECRET=your-super-secret-jwt-key-change-in-production
